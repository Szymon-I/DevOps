### Pokemon api
Pokemon api based on FastApi framework.

### Running
Make sure you have docker and docker compose installed.
Run all containers with
> make run-all

All available commands are in `makefile`

### Docs
After running server docs for whole api can be accessed under address:
- 'localhost:8000/docs' -> swagger/openapi
- 'localhost:8000/redoc' -> redoc
